# Frozen candidate: ghost-structural-nested-return-guard (v0.3.12)

Experimental nested-hub parent preservation plus the v0.3.9 exact-repeat return-cycle guard. Not the product default.

Historical C1 freeze remains `../ghost-structural-v0.3.6/freeze.json`.
Historical guard freeze remains `../ghost-return-cycle-guard-v0.3.9/freeze.json`.

The only semantic delta is absorbing a hub branch click into an already open sequence while `commitment_left > 0` and `returning` is false. No parent stack. No threshold N.
