# v0.3.22 post-escape sink analysis

Analysis only. This round does not modify the v0.3.21 candidate, does not modify v0.3.20 target or generator semantics, does not generate targets, and does not change the product default.

v0.3.21 Outcome C stands at `deda60a7ef7494466d12598b9418234c09b543f4`. Return-Waypoint Frontier Escape engaged on all four frozen positive targets. Warehouse and Booking recovered Guard templates 5/8/9. Campus and Studio did not. The question is why the same first escape diverges after it succeeds.

`protocol.json` freezes H1–H4, the dominance threshold 0.60, the eight cells (four targets at budgets 240 and 480, seed 1), and the measurement definitions before those cells run. `executed` stays false in this preregistration. `v03121-post-escape-audit.json` and `static-scc-analysis.json` are recomputed from the committed v0.3.21 budget-120 evidence and the frozen topology. They are not new exploration.

The suffix cut is the earliest sequence-scoped return-cycle abandonment after the first waypoint escape. The horizon cycle that reaches the recurrent component is reported separately. A component is `closed_scc` only when the navigation graph has no edge leaving it. An exit that exists and is not required for the label is `policy_attractor_with_static_exit`.

No repair is implemented in this round.
