# v0.3.26 untried sibling before return

Fresh validation of one narrow repair. It does not edit the frozen v0.3.24 candidate, and it does not change the product default.

`protocol.json` is committed with `executed: false` before the new applications are generated and before any policy run on them.

The repair: while a sequence is returning, an unstarted branch click on the current hub is taken before the return action. The return resumes after that child comes back. This does not add a horizon handoff.

The four new positives keep a single spine plus one board. The score-mismatch page is a sibling on the chart hub. That is not the v0.3.25 desk placement. The two negatives are a shallow rack and a shallow window. All twenty-two applications used in v0.3.7 through v0.3.25 are excluded as fresh targets.

The matrix is 28 fresh cells, 10 historical cells, and 4 inspected cells on the v0.3.25 positives. The inspected cells are reported beside the outcome letter and do not change it.
