# v0.3.25 fresh transfer of the frozen episode-drain candidate

Fresh validation. It is not another Campus/Studio repair, and it does not change the product default.

`protocol.json` is committed with `executed: false` before the new applications are generated and before any policy run on them.

The four positives use a single spine plus one board workflow. That is not the v0.3.20 dual mid/child column graph. The two negatives are a shallow shelf and a shallow counter. All sixteen applications used in v0.3.7 through v0.3.24 are excluded as fresh targets.

The candidate stays `ghost-structural-episode-drain-epoch-guard` at the v0.3.24 freeze. Guard is `ghost-structural-return-guard`. The matrix is 28 fresh cells plus 10 historical cells.
